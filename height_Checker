class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        temp=heights.copy()
        temp.sort()
        c=0
        for i in range(len(heights)):
            if temp[i]!=heights[i]:
                c+=1
        return c
