class Solution:
    def minimumCost(self, nums: List[int]) -> int:
        r=[]
        for i in range(1,len(nums)):
            r.append(nums[i])
        r.sort()
        ans=nums[0]+r[0]+r[1]
        return ans
